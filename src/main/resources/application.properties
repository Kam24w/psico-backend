# PSICO VIRTUAL - APPLICATION PROPERTIES

spring.application.name=psico-backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# --- BASE DE DATOS (PostgreSQL / Supabase) ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-us-west-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.viuesoohxisioluoveap}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:psico2026...}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- JWT ---
jwt.secret=${JWT_SECRET:psicovirtual_clave_secreta_jwt_muy_larga_y_segura_2026}
jwt.expiration=86400000


# --- GROQ (LLaMA AI) ---
groq.api.key=${GROQ_API_KEY:sin-configurar}
groq.api.url=https://api.groq.com/openai/v1/chat/completions


# --- SERVIDOR ---
server.port=${PORT:8080}

# --- LOGGING ---
logging.level.com.psico=INFO
logging.level.org.springframework.security=WARN