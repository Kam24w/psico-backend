# =============================================
# PSICO VIRTUAL - APPLICATION PROPERTIES
# =============================================

spring.application.name=psico-backend

# --- BASE DE DATOS (MySQL / Railway) ---
# Railway provee MYSQL_URL con prefijo mysql://, hay que convertirlo a jdbc:mysql://
spring.datasource.url=${DATABASE_URL:jdbc:mysql://centerbeam.proxy.rlwy.net:40171/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:tgGlNwEzZvfTSEMxktqyGQuBJmBnzDpg}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT ---
jwt.secret=${JWT_SECRET:psico-virtual-secret-key-2024-muy-segura-32chars}
jwt.expiration=86400000

# --- GEMINI (Google AI) ---
gemini.api.key=AIzaSyBOetCEpBcBF0N7jZT5WrlV8fVKg-FnqHo
gemini.api.model=${GEMINI_MODEL:gemma-4-26b-a4b-it}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/${gemini.api.model}:generateContent

# --- SERVIDOR ---
server.port=${PORT:8080}

# --- LOGGING ---
logging.level.com.psico=INFO
logging.level.org.springframework.security=WARN
